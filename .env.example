# ICP Federated Learning Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# ADMIN CONFIGURATION
# =============================================================================
# The admin has full control over the system
ICP_ADMIN_PRINCIPAL_ID=your-admin-principal-id-here
ICP_ADMIN_SEED_PHRASE="your twelve word admin seed phrase here"

# =============================================================================
# SERVER CONFIGURATION  
# =============================================================================
# The federated learning server coordinates training
ICP_SERVER_PRINCIPAL_ID=your-server-principal-id-here
ICP_SERVER_SEED_PHRASE="your twelve word server seed phrase here"

# =============================================================================
# CLIENT CONFIGURATION
# =============================================================================
# Each client should have unique credentials
ICP_CLIENT_PRINCIPAL_ID=your-client-principal-id-here
ICP_CLIENT_SEED_PHRASE="your twelve word client seed phrase here"

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================
# Network to connect to (local, ic, or custom)
ICP_NETWORK=local

# Canister ID (will be auto-generated after deployment)
ICP_CANISTER_ID=your-canister-id-here

# =============================================================================
# FEDERATED LEARNING CONFIGURATION
# =============================================================================
# Default server address for Flower clients
FL_SERVER_ADDRESS=127.0.0.1:8080

# Default number of training rounds
FL_ROUNDS_DEFAULT=3

# Minimum number of clients required for training
FL_MIN_CLIENTS=1

# Client connection timeout (seconds)
FL_CLIENT_TIMEOUT=120

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# Enable/disable automatic client approval (for development only)
FL_AUTO_APPROVE_CLIENTS=false

# Maximum number of pending clients
FL_MAX_PENDING_CLIENTS=100

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Create dfx identities:
#    dfx identity new admin --storage-mode password-protected
#    dfx identity new server --storage-mode password-protected  
#    dfx identity new client1 --storage-mode password-protected
#
# 2. Get principal IDs:
#    dfx identity use admin && dfx identity get-principal
#    dfx identity use server && dfx identity get-principal
#    dfx identity use client1 && dfx identity get-principal
#
# 3. Export seed phrases (optional, for backup):
#    dfx identity export admin > admin.pem
#    dfx identity export server > server.pem
#    dfx identity export client1 > client1.pem
#
# 4. Deploy canister and get canister ID:
#    dfx deploy
#    dfx canister id fl_cvd_backend_backend
#
# 5. Initialize admin role:
#    dfx canister call fl_cvd_backend_backend init_admin
