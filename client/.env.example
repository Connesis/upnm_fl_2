# Client-specific Environment Configuration
# Copy this file to .env and fill in your client-specific values

# =============================================================================
# CLIENT IDENTITY CONFIGURATION
# =============================================================================
# Each client must have unique credentials
ICP_CLIENT_PRINCIPAL_ID=your-unique-client-principal-id
ICP_CLIENT_SEED_PHRASE="your unique twelve word client seed phrase"

# Client identity name (for dfx identity management)
ICP_CLIENT_IDENTITY_NAME=client1

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================
# Network to connect to (should match server configuration)
ICP_NETWORK=local

# Canister ID (should match server configuration)
ICP_CANISTER_ID=your-canister-id-here

# =============================================================================
# FEDERATED LEARNING CONFIGURATION
# =============================================================================
# Server address to connect to
FL_SERVER_ADDRESS=127.0.0.1:8080

# Client-specific dataset path (optional, can be overridden by command line)
FL_DATASET_PATH=dataset/clients/client1_data.csv

# Client timeout for server connection (seconds)
FL_CONNECTION_TIMEOUT=30

# =============================================================================
# CLIENT METADATA
# =============================================================================
# Optional client information for registration
CLIENT_NAME=Healthcare Provider 1
CLIENT_ORGANIZATION=Hospital ABC
CLIENT_LOCATION=City, Country
CLIENT_CONTACT_EMAIL=admin@hospital-abc.com

# =============================================================================
# SETUP INSTRUCTIONS FOR THIS CLIENT
# =============================================================================
# 1. Create unique dfx identity for this client:
#    dfx identity new client1 --storage-mode password-protected
#
# 2. Get this client's principal ID:
#    dfx identity use client1
#    dfx identity get-principal
#
# 3. Copy the principal ID to ICP_CLIENT_PRINCIPAL_ID above
#
# 4. Export seed phrase for backup (optional):
#    dfx identity export client1 > client1_backup.pem
#
# 5. Start the client:
#    uv run python client.py --dataset your-dataset.csv
#
# 6. Wait for admin approval before participating in training
